{% extends "KnpIpsumBundle::layout.html.twig" %}

{% block title "Pagerfanta Tutorial" %}

{% block content %}
    <h1>Paginated list of things</h1>

    <p>Here is the list of things from your database:</p>

    {% if paginator|length > 0 %}
        <ul>
        {% for thing in paginator %}
            <li>* {{ thing.identifyYourselfPlease }}</li>
        {% endfor %}
        </ul>
        <div class="pagerfanta">
            {{ pagerfanta(paginator, 'default') }}
        </div>
    {% else %}
        <p>− There are no things yet!</p>
    {% endif %}
{% endblock %}

{% set code = code(_self) %}
